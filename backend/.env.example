# Postgres local (docker-compose)
DATABASE_URL=postgresql+psycopg://iced:iced@localhost:5432/iced

# Supabase (ejemplo): reemplazar solo esta variable, nada de codigo cambia
# DATABASE_URL=postgresql+psycopg://postgres:TU_PASSWORD@db.TU_REF.supabase.co:5432/postgres

# CORS: origenes permitidos para el frontend Vite
CORS_ORIGINS=http://localhost:5173

FLASK_DEBUG=1
